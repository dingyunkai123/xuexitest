package shejimoshi.xingweimoshi.zerenlian;/** * @Title: Handler * @Package shejimoshi.xingweimoshi.zerenlian * @Author: dingyunkai * @Description 抽象处理者 * @CreateTime: 2023/6/7 20:29 */public abstract class Handler {    protected final static int num_one = 1;    protected final static int num_three = 3;    protected final static int num_seven = 7;    //该领导处理的请假区间    private int numStart;    private int numEnd;    //声明后继者    private Handler nextHandler;    public Handler(int numStart) {        this.numStart = numStart;    }    public Handler(int numStart, int numEnd) {        this.numStart = numStart;        this.numEnd = numEnd;    }    public void setNextHandler(Handler handler) {        this.nextHandler = handler;    }    protected abstract void hanleLeave(LeaveRequest  leaveRequest);    public final void submit(LeaveRequest request){        this.hanleLeave(request);        if (this.nextHandler != null && request.getNum() > this.numEnd){            this.nextHandler.submit(request);        }else {            System.out.println("结束");        }    }}